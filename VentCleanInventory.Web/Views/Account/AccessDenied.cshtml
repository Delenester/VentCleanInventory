@{
    ViewData["Title"] = "Доступ запрещён";
}

<div class="alert alert-danger">
    <div class="fw-semibold mb-1">@ViewData["Title"]</div>
    <div>У вас нет прав для просмотра этой страницы.</div>
</div>

